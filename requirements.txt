streamlit==1.47.0
# CPU build: much smaller install, and Streamlit Cloud has no GPU anyway.
# Pinned exactly: 2.20.0 has wheels for Python 3.9-3.13, so the resolver cannot
# wander onto a version with no wheel for the host's interpreter.
# Models were saved with Keras 3.5.0; loading verified on Keras 3.14.
tensorflow-cpu==2.20.0
numpy>=1.26,<2.3
pillow>=10
opencv-python-headless>=4.9
huggingface-hub>=0.25
